# Exclude everything by default
**

# Include only what's needed for the extension to run

# Include extension code
!dist/extension.js
!dist/extension.js.map
!dist/commands.js
!dist/commands.js.map
!dist/globals.js
!dist/globals.js.map

# Include server code
!dist/src/server/**/*.js
!dist/src/server/**/*.js.map

# Note: Python files no longer needed (using standard MCP format)

# Include webview UI build output
!webview-ui/dist/**

# Include MCP Inspector UI files (minimal bundle)
!dist/mcp-inspector-ui/**

# Include package metadata
!package.json
!README.md
!LICENSE.md
!resources/**

# Include only essential node_modules
!dist/node_modules/**

# Include Phoenix bundle
!dist/phoenix.pyz

# Exclude node_modules that might be included by the above patterns
webview-ui/**/node_modules/**

# Exclude temporary build files
**/*.tsbuildinfo
**/.DS_Store
.temp/**

# Include only essential .clay files for MCP servers
!.clay/mcp/*.md
!.clay/mcp-servers/*.md
!.clay/mcp-servers/*.mjs
!.clay/mcp-servers/*.js
!.clay/mcp-servers/*.ts
# Exclude heavy directories
.clay/mcp-servers/.venv/**
.clay/mcp-servers/__pycache__/**
.clay/mcp-servers/node_modules/**